apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: gateway-internal
  namespace: network
spec:
  endpoints:
    - dnsName: internal.kube.ag
      recordType: A
      targets:
        - 172.17.8.251

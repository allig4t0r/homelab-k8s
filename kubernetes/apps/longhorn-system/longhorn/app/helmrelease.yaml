apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: longhorn
spec:
  chartRef:
    kind: OCIRepository
    name: longhorn
  interval: 1h
  values:
    defaultSettings:
      defaultDataPath: /var/mnt/longhorn
    csi:
      kubeletRootDir: /var/lib/kubelet
